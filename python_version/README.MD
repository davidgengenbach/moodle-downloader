## Instructions

```bash
pip3 install -r requirements.txt
touch user.txt
# Enter your credentials in this user.txt
# TUID and Password on their own lines
python3 crawl.txt
# Your Files will now reside in out/{course_title}
```